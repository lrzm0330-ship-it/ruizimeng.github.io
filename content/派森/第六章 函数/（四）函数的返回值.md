1、函数中的==return语句==会在函数结束时将数据返回给程序，同时让程序回到函数被调用的位置继续执行
- <mark style="background: #FF5582A6;">return：把函数内部算出来的结果，交给函数外面使用
</mark>
*返回多个值.py*
![[截屏2026-04-29 09.30.02.png|499]]
- 简而言之，append()的返回值是None，也就是它不具有赋值的功能，**改变对象，不返回结果**，正确写法：==I.append( 'a' )==

2、如果函数使用return语句返回了==多个值==，那么这些值将被保存到<mark style="background: #FF5582A6;">元组</mark>中
![[截屏2026-04-29 09.46.18.png|413]]
- 输出：（160，40）
函数内部：到return结束；result=m（）及以下都属于函数外部内容
