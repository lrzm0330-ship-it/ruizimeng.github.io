全局变量可以在整个程序的范围内起作用，它不会受到函数范围的影响
全局变量在函数内部只能被访问，而无法直接修改
①
![[截屏2026-04-29 10.05.49.png|578]]
②
![[截屏2026-04-29 10.07.03.png|571]]

1、**在函数内部修改number，==python会默认为number是一个新的局部变量（不是替代！是另一个，此时外部一个全局变量number，函数内部一个局部变量number）==，此时你在没有定义number的前提下就进行使用，python会报错**
2、**global number 的含义：告诉 Python：这个 number 不是新的变量，用的是函数外面的那个 number**
3、**global 本质作用：让函数内部可以“使用并修改函数外的变量 number**
