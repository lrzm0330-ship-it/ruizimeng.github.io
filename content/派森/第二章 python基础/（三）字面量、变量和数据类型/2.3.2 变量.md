标识内存单元的标识符由称为==变量名==，Python中通过赋值运算符=将内存单元中存储的数值与标识符==绑定==，即定义变量。
具体语法格式：<mark style="background: #FF5582A6;">标识符=值</mark>

 <mark style="background: #ADCCFFA6;">Q、字面量和变量区别？</mark>
- eg.x=100中，x是变量，100是字面量
<mark style="background: #ADCCFFA6;">Q、字面量、变量、值和标识符的区别？</mark>
![[截屏2026-04-02 19.00.19.png|548]]

*==标识符是取名的规则，变量是名字本身，字面量是写出数据的方式，值是数据在内存中的真实存在