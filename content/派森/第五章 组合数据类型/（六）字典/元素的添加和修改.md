##### (1)元素的添加
字典支持通过为==指定的键赋值==或使用==update（）==方法==添加或修改==元素，下面分别介绍如何添加和修改元素.
格式：
- 字典变量[键]=值 #通过键添加元素
- dict.update(key=value) #使用update（）添加元素
![[截屏2026-04-23 19.31.42.png|608]]
##### （2）元素的修改
修改字典元素的本质是通过键获取值，再重新对元素进行赋值。修改元素的操作与添加元素的操作相同（格式相同）