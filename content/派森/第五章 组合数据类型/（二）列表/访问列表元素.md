列表中的元素可以通过==索引==或==切片==进行访问，也可以在==循环中==依次访问

>[!note]
>**切片和索引区别？**
>索引：用来取一个特定位置的“==单个==”元素
>切片：用来取一个范围的“==多个==元素”，形成一个子序列

![[截屏2026-04-15 10.43.14.png|445]]
==*如果写的是：I [ :2 ]=[ [] , [] ]，输出结果是[ [] , [] , [] , 'd' , 'e' , 'f' ]*==

>[!danger]
>**切片赋值和索引赋值区别？**
>索引赋值：右边当一个元素放进去
>切片赋值：右边当一个序列展开替换
