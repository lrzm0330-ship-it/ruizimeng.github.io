1、元组的表现形式为一组包含在原括号==“ ()“==中、由逗号分隔的元素，元组中元素的个数、类型不受限制。除了使用()构建元组外，还可以使用内置函数 ==tuple()== 构架元组 
- t1=() #使用（）构建元组
- t2=tuple() #使用tuple（）构建元组
2、当使用圆括号“（）”创建元组时，如果元组中只包含一个元素，那么需要==在该元素的后面添加逗号==，而从保证python解释器能够识别其为元组类型
![[截屏2026-04-16 19.43.06.png|326]]
3、元组支持以==索引==和==切片==方式访问元组的元素，也支持在循环中==遍历==元组
- 索引与切片
![[截屏2026-04-16 19.49.21.png|308]]
- 遍历元组
(pic)