### 1、打开文件
内置函数open（）用于打开文件，该方法的声明如下：
open（file，mode=' r '，buffering=-1）
	**file**：文件的路径
	**mode**：设置文件的打开方式
		r：以只读方式打开文件（mode参数的默认值）
		w：以只写方式打开文件
		a：以追加方式打开文件
		b：以二进制形式打开文件（通常与前三者联合使用）
		+：以更新的方式打开文件（可读可写）（通常与前三者联合使用）
	**buffering**：设置访问文件的缓冲方式。取值为0（无缓冲）或1（行缓冲）（作用：控制文件读写时数据是否先暂存在内存中，以及暂存多少）（值为-1:不手动指定，  让 Python 自动决定最合适的缓冲方式）

| **打开**模式 |    名称    |                       描述                       |
| :------: | :------: | :--------------------------------------------: |
|   **r/rb**   |   **只读模式**   |     **以只读的形式打开文本文件/二进制文件，若文件不存在或无法找到，文件打开失败**      |
|   **w/wb**   |   **只写模式**   |    **以只写的形式打开文本文件/二进制文件，若文件已存在，则重写文件，否则创建新文件**     |
|   **a/ab**   |   **追加模式**   | **以只写的形式打开文本文件/二进制文件，只允许在该文件末尾追加数据，若文件不存在，则创建新文件** |
|  **r+/rb+**  | **读取（更新）模式** |       **以读/写的形式打开文本文件/二进制文件，若文件不存在，文件打开失败**        |
|  **w+/wb+**  | **写入（更新）模式** |        **以读/写的形式打开文本文件/二进制文件，若文件已存在，则重写文件**        |
|  **a+/ab+**  | **追加（更新）模式** |  **以读/写的形式打开文本/二进制文件，只允许在文件末尾添加数据，若文件不存在，则创建新文件**  |
【返回值】若open（）函数调用成功，返回一个文件对象
- file = open ( ' E:\ \b.txt ' ) 以只读的方式打开E盘的文本文件b.txt 
- file = open ( ' c.txt ', 'w' ) 以只写方式打开当前目录的文本文件c.txt
【返回值】若待打开的文件不存在，文件打开失败，程序会抛出异常，并打印错误信息
### 2、关闭文件
python可通过close（）方法关闭文件，也可以使用with语句实现文件的自动关闭
##### （1）close（）方法
close（）方法是文件对象的内置方法：file.close（）
- 举例
![[截屏2026-05-08 11.14.34.png|261]]
##### （2）with语句：with open（...） as ...：
with语句可预定义清理操作，以实现文件的==自动==关闭
- 举例
![[截屏2026-05-08 11.19.34.png|311]]
	① as f表示：把打开后的文件对象交给变量 f
	②冒号表示：下面缩进的代码属于 with 管理范围
	③pass含义：先什么都不做，占个位置；因为python语法规定
##### （3）为什么要及时关闭文件？
①计算机中可打开的文件数量是有限的
②打开的文件占用系统资源
③若程序因异常关闭，可能产生数据丢失