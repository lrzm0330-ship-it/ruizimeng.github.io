1、字符串中可能会包含一些无用的字符（如空格），在处理字符串之前往往需要先删除这些无用的字符。Python中的==strip()==、==Istrip()==和==rstrip()==方法可以删除字符串中的指定字符

![[截屏2026-04-08 10.53.05.png|454]]
2、特定情况下会对英文单词的大小写形式进行要求，表示特殊简称时全字母大写，如CBA；表示月份、周日、节假日时每个单词首字母大写，如Monday
python中支持字母大小写转换的方法有upper()、lower()、capitalize()和title()
*==文件传输还有一张图==*